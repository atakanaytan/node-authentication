/*const express = require('express');
const  index = express();
//const app = require('./app');

index.use("/assesments", express.static(__dirname + '/assesments'));
index.set('view engine', 'ejs');
index.use(express.static('./assesments'));
index.get('/', function (req, res) {
    res.render('login');
});


index.listen(3000);

console.log('you are listening on 8000 port');
*/
